name: sqlite_explorer
description: Package to display sqlite data tables
version: 0.4.0+1

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  after_layout: ^1.0.7+2
  auto_size_text: ^2.1.0
  cupertino_icons: ^1.0.0
  flutter:
    sdk: flutter
  theme_packge:
    path: ../theme_package
  shared_preferences: ^0.5.12+2
  sqlite_controller:
    path: ../sqlite_controller

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
